//* to create the employees_pb.js run in the command line the "protoc --js_out=import_style=commonjs,binary:. employees.proto"

syntax = "proto3";

message Employee {
    int32 id = 1;
    string name = 2;
    float salary = 3;
}

message Employees {
    repeated Employee employees = 1; 
}